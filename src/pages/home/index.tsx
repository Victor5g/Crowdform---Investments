import { SafeAreaView, Text } from "react-native";

import style from "./style";

const Home = () => {
  return (
    <SafeAreaView style={style.container}>
      <Text>Home</Text>
    </SafeAreaView>
  );
};

export default Home;
